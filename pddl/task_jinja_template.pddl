(define (problem sokoban_path)
   (:domain sokoban)
    (:requirements :strips :typing )
   (:objects
       ; p1-1 p2-2 - location
       {{ objects }}
   )
   (:init

        ; ================================================================================
        ; PATH
        ; ================================================================================
        ; (path p1-1 p2-2)         (path p2-2 p1-1)
        {{ path }}

        ; ================================================================================
        ; BOX
        ; ================================================================================
        ; (is_box_at p1-1)
        {{ is_box_at }}

        ; ================================================================================
        ; PLAYER
        ; ================================================================================
        ;(at p2-2)
        {{ player }}

   )
   ; (:goal  (is_box_at p2-2))
   (:goal
       (and {{ goal }})
   )

)
